---
import { navItems2 } from "../data/dataInfo";
---

<div
  class="fixed flex items-center justify-center z-20 gap-4 p-4 text-white rounded-md"
>
  <!-- Botón menú -->
  <button
    class="flex justify-center p-2 bg-gray-700 rounded-md hover:bg-gray-600 transition-colors"
    onclick="document.getElementById('dropdown-menu').classList.toggle('hidden')"
  >
    <img src="/icons/menu-2.svg" alt="icon-menu" class="w-6 h-6">
  </button>

  <!-- Menú desplegable -->
  <div
    id="dropdown-menu"
    class="hidden flex flex-wrap items-center text-white rounded-md shadow-xs"
  >
    <!-- Mapeo opciones -->
    {
      navItems2.map((item) => (
        <a
          href={item.href}
          class="flex border-white/10 items-center px-4 py-2 text-sm font-medium text-white bg-gray-700 border border-gray-200 hover:bg-gray-500 hover:text-blue-700 dark:bg-gray-800 dark:border-gray-700 dark:text-white dark:hover:text-white dark:hover:bg-gray-700 "
        >
          <img src={item.icon} alt={`${item.text} icon`} class="w-6 h-6 " />
          {item.text}
        </a>
      ))
    }
  </div>
</div>
